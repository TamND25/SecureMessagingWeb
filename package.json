{
  "scripts": {
    "install-all": "cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build && cp -r frontend/build backend/",
    "start": "cd backend && node server.js"
  }
}
