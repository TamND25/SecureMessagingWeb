{
  "scripts": {
    "install-all": "cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && REACT_APP_API_URL=$REACT_APP_API_URL npm run build && cd .. && cp -r frontend/build backend/",
    "start": "cd backend && node server.js"
  }
}
